@using Microsoft.AspNetCore.Identity
@using MyTvSeries.Domain.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@*@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/Index", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a asp-area="" asp-controller="Profile" asp-action="Index" asp-route-id="@User.Identity.Name">@UserManager.GetUserName(User)</a>
            </li>
            <li>
                <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Account settings</a>
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link">Logout</button>
            </li>
            <li>
                <div class="dropdown">
                    <button class="dropbtn">@UserManager.GetUserName(User)</button>
                    <div class="dropdown-content">
                        <a asp-area="" asp-controller="Profile" asp-action="Index" asp-route-id="@User.Identity.Name">Profile</a>
                        <a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Account settings</a>
                        <button type="submit" class="btn btn-link navbar-btn navbar-link">Logout</button>
                    </div>
                </div> 
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li><a asp-area="Identity" asp-page="/Account/Register">Register</a></li>
        <li><a asp-area="Identity" asp-page="/Account/Login">Login</a></li>
    </ul>
}*@
